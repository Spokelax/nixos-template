# Hosts

VM-specific configuration. This folder is gitignored.

## Setup

Run `./onboarding.sh` or see `host.example.nix` for manual setup.

## Structure

```text
hosts/
├── default.nix   # Host index
├── config.nix    # Host settings
└── hardware.nix  # Hardware config
```
